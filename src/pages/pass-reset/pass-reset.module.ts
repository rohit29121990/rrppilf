import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PassResetPage } from './pass-reset';

@NgModule({
  declarations: [
    PassResetPage,
  ],
  imports: [
    IonicPageModule.forChild(PassResetPage),
  ],
})
export class PassResetPageModule {}
